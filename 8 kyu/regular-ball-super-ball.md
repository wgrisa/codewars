# regular-ball-super-ball
// https://www.codewars.com/kata/regular-ball-super-ball/solutions/javascript/me/best_practice


```
var Ball = function(ballType) {
  this.ballType = ballType === 'super' ? 'super' : 'regular';
};
```

```
var Ball = function(ballType) {
  this.ballType=ballType==='super'?'super':'regular';
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType === "super" ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = (ballType === 'super') ? 'super' : 'regular';
};
```

```
var Ball = function(ballType) {
  this.ballType = (ballType === "super")?"super":"regular";
};
```

```
var Ball = function(ballType) {
  // your code goes here
  this.ballType = (ballType === 'super'?'super':'regular')
};
```

```
var Ball = function(ballType) {
  // your code goes here
  this.ballType = (ballType === "super") ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  // your code goes here
  this.ballType = ( ballType === "super" ? "super" : "regular" );
};
```

```
var Ball = function(ballType) {
  this.ballType = (ballType === "super") ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
this.ballType=(ballType==="super")?"super":"regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType==="super"? "super" : "regular"
};
```

```
var Ball = function(ballType) {
  this.ballType = (ballType === "super") ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType === 'super' ? 'super': 'regular';
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType === "super" ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  // your code goes here
  this.ballType = (ballType === "super")?"super":"regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = (ballType === "super") ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType === "super" ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType === "super" ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  // your code goes here
  this.ballType = (ballType === "super") ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  // your code goes here
  this.ballType=(ballType==="super")?"super":"regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = (ballType === 'super') ? 'super' : 'regular';
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType === "super" ? "super" : "regular";
};
```

```
var Ball = function(ballType) {
  this.ballType = ballType === "super" ? "super" : "regular";
};
```
