# find-numbers-which-are-divisible-by-given-number
// https://www.codewars.com/kata/find-numbers-which-are-divisible-by-given-number/solutions/javascript/me/best_practice


```
const divisibleBy = (numbers, divisor) => numbers.filter(x => x % divisor === 0);
```

```
const divisibleBy = (numbers, divisor) => numbers.filter(x => x%divisor === 0)
```

```
const divisibleBy = (numbers, divisor) => numbers.filter(x => x % divisor === 0);

```

```
const divisibleBy = (numbers, divisor) => numbers.filter(x => x % divisor === 0)
```

```
const divisibleBy = (numbers, divisor) => numbers.filter(x=>x%divisor===0)
```

```
const divisibleBy = (numbers, divisor) => numbers.filter(x => x % divisor === 0)
```

```
const divisibleBy = (numbers, divisor) => numbers.filter( x => x % divisor === 0 );
```

```
const divisibleBy = (numbers, divisor) => numbers.filter(x => x % divisor === 0)
```
