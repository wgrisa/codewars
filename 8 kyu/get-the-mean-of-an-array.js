# get-the-mean-of-an-array
// https://www.codewars.com/kata/get-the-mean-of-an-array/solutions/javascript/me/best_practice


```
function getAverage(marks){
  var sum = 0;
  for(var i = 0;i<marks.length;i++){
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
}

```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
  sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++){
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++)
        sum += marks[i];

  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i<marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++){
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum=0;
  for(var i =0;i<marks.length;i++){
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0 ; i < marks.length ; i++){
    sum+= marks[i];
  }
return Math.floor(sum/marks.length)
}

```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }

  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks) {
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
    return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;

  for(var i = 0; i < marks.length; i++) {
      sum += marks[i]
  }

  return Math.floor(sum / marks.length)
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i = 0; i<marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i]
  }
  return Math.floor(sum / marks.length)
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum =0;
  for(var i=0;i<marks.length;i++)
    sum+=marks[i];
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i = 0; i < marks.length; i++){
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i=0; i< marks.length; i++) {
        sum += marks[i];
        }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
var sum= 0;
  for (var i = 0; i<marks.length; i++) {
    sum+=marks[i]
  };
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum=0;
  for(var i=0;i<marks.length;i++){
   sum+=marks[i];
   }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for(var i = 0; i<marks.length; i++){
     sum += marks[i];
  }
  return  Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;

  for(var i = 0; i < marks.length; i++) {

  sum += marks[i];

  }

  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;

  for(var i = 0; i<marks.length; i++) {
    sum += marks[i];
  }
    return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum =0;
 for (var i=0; i <marks.length;i++) {
  sum += marks[i]
}
return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum=0;
  for(var i=0;i<marks.length;i++){
    sum += marks[i];
  }
  return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  var sum = 0;

  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }

  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }

  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
var sum=0;
  for (var i=0;i<marks.length;i++) {
  sum+=marks[i];

  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks)
{
    var sum = 0;
    for (var i = 0; i < marks.length; i++)
    {
        sum += marks[i];
    }
    return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
    var sum = 0;
    for (var i = 0; i < marks.length; i++) {
        sum += marks[i];
    }

    return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
 var sum =0;
 for(var i=0;i<marks.length;i++)
   sum += marks[i];

  return Math.floor(sum/marks.length);

}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
   var sum = 0
  for( var i = 0; i < marks.length; i++ ){
      sum += marks[i];
  }

  return  Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
var sum = 0;
 for (var i = 0; i < marks.length; i++) {
  sum += marks[i];
 }
  return Math.floor(sum / marks.length);

}
```

```
function getAverage(marks){
  var sum = 0;

  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }

  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
var sum = 0
  for(var i = 0; i < marks.length; i++) {
  sum += marks[i]
  }
  return Math.floor(sum/marks.length)

  //TODO : calculate the downwar rounded average of the marks array
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
 var sum = 0
 for (var i = 0; i < marks.length; i++) {
 sum += marks[i]
 }
 return Math.floor(sum / marks.length)
}
```

```
function getAverage(marks){

  var sum = 0;

  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }

  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0
  for (var i=0;i<marks.length;i++){
      sum += marks[i]
  }
  return Math.floor(sum/(marks.length))
}
```

```
function getAverage(marks){
 var sum = 0;
 for (var i=0; i<marks.length; i++) {
 sum += marks[i];
 }
 return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum=0;
  for(var i=0; i<marks.length; i++){
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
    var sum = 0;
  for (var i = 0; i<marks.length; i++){
    sum += marks[i];

  }
    return Math.floor(sum/marks.length);
    }
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for(var i = 0; i < marks.length; i++){
    sum += marks[i]
  }

  return Math.floor(sum / marks.length)
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i ++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i =0; i< marks.length;i++) {
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i =0; i < marks.length; i++) {
    sum += marks[i]
  }
  return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i=0;i<marks.length;i++){
    sum += marks[i]
  }
  return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
var sum = 0;
for(var i = 0; i < marks.length; i++){
  sum += marks[i];
}
return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
    var sum =0;
  for (var i = 0; i < marks.length; i++) {
      sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum=0;
  for(var i=0;i<marks.length;i++){
  sum+=marks[i];
  }
return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i=0;i<marks.length;i++)
  {
    sum += marks[i];
  }

  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i=0;i<marks.length;i++){
  sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum=0;
  for(var i=0;i<marks.length;i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++){
    sum += marks[i]
  }
  return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum=0;
  for(var i=0;i<marks.length;i++){
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i =0; i < marks.length; i++)
    sum += marks[i];

    return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){

  //TODO : calculate the downwar rounded average of the marks array
    var sum = 0

    for (var i=0;i<marks.length;i++){
    sum += marks[i];
    }
    return Math.floor(sum / marks.length);

}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i=0; i<marks.length; i++)
  sum += marks[i];
  return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
var sum = 0;
for (var i = 0; i<marks.length; i++){
sum+= marks[i];
}
return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  var sum = 0
  for (var i = 0; i < marks.length; i++) {
  sum += marks[i];

}

return Math.floor(sum / marks.length)
}


```

```
function getAverage(marks){
  var sum =0
  for(var i=0;i<marks.length;i++){
    sum += marks[i]
  }
return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  var sum=0;
  for (var i=0; i<marks.length; i++) {
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
var sum = 0;
  for (var i=0; i<marks.length; i++) {
   sum += marks[i]
  }
  return Math.floor(sum/marks.length)
}
```

```
  function getAverage(marks){
    var sum=0;
  for(var i=0;i<marks.length;i++){
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);

}
```

```
function getAverage(marks){
  var sum=0;
  for(var i=0; i<marks.length;i++){
    sum+=marks[i];}
  return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i<marks.length; i++){
    sum += marks[i]
  }
  return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){

 var sum=0;
  for(var i=0;i<marks.length;i++)
 {   sum+=marks[i];
 }
  return Math.floor(sum/marks.length);



//TODO : calculate the downwar rounded average of the marks array
}
```

```
function getAverage(marks){
  var sum = 0
  for (var i = 0; i < marks.length; i++){
     sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i]
  }
  return Math.floor(sum / marks.length)
}


  //TODO : calculate the downwar rounded average of the marks array

```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
    var sum = 0;

    for(var i= 0; i < marks.length; i++){
        sum += marks[i]
    }

    return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){

  var sum = 0

  for(var i = 0; i < marks.length ;i++){

        sum += marks[i];

  }

    return Math.floor(sum/marks.length)
}

```

```
function getAverage(marks){
  var sum = 0;

  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }

  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
 return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
 var sum = 0;
    for (var i = 0; i < marks.length; i++) {
        sum += marks[i];
       }
    return Math.floor(sum/marks.length);

}
```

```
function getAverage(marks){
  var sum = 0;
for (var i = 0; i < marks.length; i++) {
    sum += marks[i]
}
  return Math.floor(sum/marks.length)
}

```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++)
    sum += marks [i];

  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for ( var i = 0; i < marks.length; i++ ) {
    sum += marks[i];
  }
  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
var sum = 0
for (var i = 0; i<marks.length; i++) {
sum += marks[i];
}
return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum=0;
  for (var i=0;i<marks.length;i++){
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
  //TODO : calculate the downwar rounded average of the marks array
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }

  return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
 var sum = 0;
 for (var i = 0 ; i < marks.length; i++){
 sum += marks[i];}
return Math.floor(sum/marks.length);

}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
  sum += marks[i]
}
return Math.floor(sum/marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks){
  var sum = 0;
  for(var i = 0; i < marks.length; i++){
    sum += marks[i];
  }
  return Math.floor(sum / marks.length);
}
```

```
function getAverage(marks) {
  var sum = 0
  for (var i = 0; i < marks.length; i++) {
    sum += marks[i]
  }
  return Math.floor(sum / marks.length)
}
```

```
function getAverage(marks){
  var sum = 0;
  for (var i=0; i<marks.length;i++) {
    sum += marks[i]
  } return Math.floor(sum/marks.length)
}
```

```
function getAverage(marks){
  var sum=0;
  for (var i=0; i<marks.length; i++){
    sum+=marks[i];
  }
  return Math.floor(sum/marks.length);
}
```
