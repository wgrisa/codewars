# find-the-remainder
// https://www.codewars.com/kata/find-the-remainder/solutions/javascript/me/best_practice


```
const remainder = (a, b) => Math.max(a,b) % Math.min(a,b);
```

```
const remainder = (a, b) => Math.max(a,b)%Math.min(a,b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
 const remainder = (a, b) => Math.max(a, b) % Math.min(a, b)
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b)
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) =>
  Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a,b) % Math.min(a,b);


```

```
const remainder=(a,b)=>Math.max(a,b)%Math.min(a,b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b)
```

```
const remainder = (a, b) =>
  Math.max(a, b) % Math.min(a, b)
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b)
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b)
```

```
const remainder = (a,b) => Math.max(a,b) % Math.min(a,b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);

```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```

```
const remainder = (a, b) => Math.max(a,b)% Math.min(a,b)
```

```
const remainder = (a, b) => Math.max(a, b) % Math.min(a, b);
```
